<!DOCTYPE html>
<html>
  <head>
    <title>EasyKitchen</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://ga.jspm.io/npm:star-rating.js@4.3.0/dist/star-rating.css">
  </head>

  <body class="<%= user_signed_in? ? 'logged' : 'unlogged' %>">

    <% if user_signed_in? %>
      <div class="page-container">
         <div class="content-container"><%= yield %>
         </div>
         <div class="footer-container"><%= render "layouts/footer" %>
         </div>
        </div>
    <% else %>
        <%= yield %>
    <% end %>

  </body>
</html>
